#ifndef SOLVE_QUADRATIC_EQUATION_H_
#define SOLVE_QUADRATIC_EQUATION_H_

typedef struct solution {
    int flag;
    double x1;
    double x2;
} solution;

typedef struct coefficients {
    double a, b, c;
} coefficients;

#endif // SOLVE_QUADRATIC_EQUATION_H_
