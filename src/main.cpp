#include "solve_equation_interactively.h"
// NOTE: As you said, such file doesn't need its own .h file.
//       Thus, how to use frontend function in the main one?

int main()
{
    coefficients coefficients;
    solve_equation_interactively(&coefficients);
}
